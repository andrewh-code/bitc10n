import sys
import os
import pyelliptic 

# class generate addresses
class GenerateAddress():
    
# use ECC to generate the private/pub key

# generate wallet address from pub key
    # algorithm to extract address from pub key (prob just use pub key)


# store wallet address in hash table with associated name

# put in keystore 

# test



